---
title: "MCP Tool Schema"
version: 3.16.0
tool_count: 37
mcp_tool_count: 29
plugin_only_tool_count: 8
generated_at: "auto"
---

# MCP Tool Schema (v3.16.0)

> **Generated file. Do not edit by hand.**
> Source: `mnemosyne/tool_schemas.py` and `mnemosyne/mcp_tools.py`.
> Regenerate with `python3 scripts/generate-docs.py`.

Mnemosyne declares **37 tools**. Of those, **29 are callable over MCP** (stdio and SSE), and **8 are implemented only in the Hermes provider** and are not reachable through the MCP server.

The split is real and worth respecting: calling a plugin-only tool over MCP raises `Unknown tool`. The counts above are derived from `mnemosyne/mcp_tools.py::_TOOL_HANDLERS`, so they cannot drift from the code.

---

## Tools available over MCP (29)

### 1. `mnemosyne_batch`

Apply multiple Mnemosyne memory mutations atomically in one tool call. Supported v1 actions: remember, update, forget, invalidate. All operations are validated before mutation; on failure the whole batch rolls back. Destructive actions require exact memory IDs. Recall/search/canonical/persona/shared-surface operations are not included in v1.

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `operations` | `array<object>` | yes | *(none)* | Ordered mutation operations to apply atomically. |
| `dry_run` | `boolean` | no | `false` |  |
| `bank` | `string` | no | *(none)* | Optional memory bank to operate on. Resolution precedence: explicit 'bank' argument, then the MNEMOSYNE_MCP_BANK environment variable, then 'default'. Omit to use MNEMOSYNE_MCP_BANK when set, otherwise the default bank (backward compatible). Named banks use separate local SQLite files and do not cross-read or cross-write. |
| `author_id` | `string` | no | *(none)* |  |
| `author_type` | `string` | no | *(none)* |  |
| `channel_id` | `string` | no | *(none)* |  |

### 2. `mnemosyne_diagnose`

Run PII-safe diagnostics on Mnemosyne installation. Checks dependencies, database state, vector search readiness, and optional vec_working migration coverage. Never includes memory content or API keys.

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `repair_vec_working` | `boolean` | no | `false` | If true, idempotently backfill missing vec_working rows from memory_embeddings. |
| `dry_run` | `boolean` | no | `false` | If true with repair_vec_working, report what would be repaired without writing. |

### 3. `mnemosyne_export`

Export all Mnemosyne memories to a JSON file for backup or migration.

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `output_path` | `string` | yes | *(none)* | File path to write the export JSON (e.g., /tmp/mnemosyne_backup.json) |
| `bank` | `string` | no | *(none)* | Optional memory bank to operate on. Resolution precedence: explicit 'bank' argument, then the MNEMOSYNE_MCP_BANK environment variable, then 'default'. Omit to use MNEMOSYNE_MCP_BANK when set, otherwise the default bank (backward compatible). Named banks use separate local SQLite files and do not cross-read or cross-write. |

### 4. `mnemosyne_forget`

Permanently delete a memory by ID.

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `memory_id` | `string` | yes | *(none)* | ID of the memory to delete |
| `bank` | `string` | no | *(none)* | Optional memory bank to operate on. Resolution precedence: explicit 'bank' argument, then the MNEMOSYNE_MCP_BANK environment variable, then 'default'. Omit to use MNEMOSYNE_MCP_BANK when set, otherwise the default bank (backward compatible). Named banks use separate local SQLite files and do not cross-read or cross-write. |

### 5. `mnemosyne_forget_canonical`

Retire a CANONICAL self-fact slot for the resolved owner in the selected local bank. This operation is private to that owner. Stamps valid_until on the current row, preserving it as local SQLite history. Returns whether a current row was retired. Nothing is deleted. Use this to remove a canonical fact (e.g. a stale preference or identity).

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `category` | `string` | yes | *(none)* | Slot group, e.g. 'identity', 'voice', 'preference' |
| `name` | `string` | yes | *(none)* | Slot key within the category, e.g. 'name', 'pronouns' |
| `bank` | `string` | no | *(none)* | Optional memory bank to operate on. Resolution precedence: explicit 'bank' argument, then the MNEMOSYNE_MCP_BANK environment variable, then 'default'. Omit to use MNEMOSYNE_MCP_BANK when set, otherwise the default bank (backward compatible). Named banks use separate local SQLite files and do not cross-read or cross-write. |

### 6. `mnemosyne_get`

Retrieve a single memory by its primary key. Pure read, no side effects. No semantic search. Returns the exact memory with the given ID or None. Use this when you already know the memory ID from a previous recall response.

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `memory_id` | `string` | yes | *(none)* | The memory ID to retrieve. |
| `bank` | `string` | no | *(none)* | Optional memory bank to operate on. Resolution precedence: explicit 'bank' argument, then the MNEMOSYNE_MCP_BANK environment variable, then 'default'. Omit to use MNEMOSYNE_MCP_BANK when set, otherwise the default bank (backward compatible). Named banks use separate local SQLite files and do not cross-read or cross-write. |

### 7. `mnemosyne_graph_link`

Declare a semantic edge between two memories in the graph. Use this to explicitly link related memories so graph traversal finds them.

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `source_id` | `string` | yes | *(none)* | Source memory ID |
| `target_id` | `string` | yes | *(none)* | Target memory ID |
| `relationship` | `string` | yes | *(none)* | Relationship label (e.g. 'references', 'caused', 'supersedes', 'related_to') |
| `weight` | `number` | no | `0.5` | Edge weight from 0.0 to 1.0 (default: 0.5) |
| `bank` | `string` | no | *(none)* | Optional memory bank to operate on. Resolution precedence: explicit 'bank' argument, then the MNEMOSYNE_MCP_BANK environment variable, then 'default'. Omit to use MNEMOSYNE_MCP_BANK when set, otherwise the default bank (backward compatible). Named banks use separate local SQLite files and do not cross-read or cross-write. |

### 8. `mnemosyne_graph_query`

Traverse the memory graph to find memories related to a seed memory. Uses multi-hop BFS through graph_edges with optional edge_type and min_weight filtering.

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `seed_memory_id` | `string` | yes | *(none)* | Memory ID to start traversal from |
| `max_hops` | `integer` | no | `2` | Maximum traversal depth (default: 2) |
| `edge_type` | `string` | no | *(unset)* | Filter by edge type (empty = all types, e.g. 'ctx', 'rel', 'syn', 'references', 'caused', 'supersedes') |
| `min_weight` | `number` | no | `0.0` | Minimum edge weight threshold (0.0 to 1.0, default: 0.0 = no filter) |
| `bank` | `string` | no | *(none)* | Optional memory bank to operate on. Resolution precedence: explicit 'bank' argument, then the MNEMOSYNE_MCP_BANK environment variable, then 'default'. Omit to use MNEMOSYNE_MCP_BANK when set, otherwise the default bank (backward compatible). Named banks use separate local SQLite files and do not cross-read or cross-write. |

### 9. `mnemosyne_hygiene_audit`

Audit the memory database for noise: terminal spam, command output, heartbeats, stack traces, secrets. Returns ranked candidates sorted by noise score. Dry-run only — does not modify the database. Use mnemosyne_hygiene_clean to act on the results.

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `limit` | `integer` | no | `200` | Max rows to scan per table (default 200, ignored when scan_all is true). |
| `offset` | `integer` | no | `0` | Row offset per table for paginated audits (default 0). |
| `scan_all` | `boolean` | no | `false` | Scan all rows in each selected table using batches instead of a single limit. |
| `batch_size` | `integer` | no | `1000` | Batch size when scan_all is true (default 1000). |
| `min_score` | `number` | no | `0.3` | Minimum noise score to include (0.0-1.0, default 0.3). |
| `tables` | `array<enum[working_memory, memories, episodic_memory, scratchpad]>` | no | *(none)* | Tables to scan. Default: working_memory + memories + episodic_memory. |
| `bank` | `string` | no | *(none)* | Optional memory bank to operate on. Resolution precedence: explicit 'bank' argument, then the MNEMOSYNE_MCP_BANK environment variable, then 'default'. Omit to use MNEMOSYNE_MCP_BANK when set, otherwise the default bank (backward compatible). Named banks use separate local SQLite files and do not cross-read or cross-write. |

### 10. `mnemosyne_hygiene_clean`

Clean noise candidates identified by mnemosyne_hygiene_audit. Actions: 'delete' (hard delete), 'archive' (decay importance to 0 + flag metadata, reversible), 'flag' (mark for review, no change). Requires confirm=true for any modification. Writes a full audit log to the hygiene_audit_log table.

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `candidates_json` | `string` | yes | *(none)* | JSON array of candidate objects from audit_noise() output. |
| `action` | `enum[delete, archive, flag, keep]` | no | `keep` | Override action for all candidates. If 'keep', uses each candidate's suggested_action. |
| `confirm` | `boolean` | no | `false` | Must be true for any modification. If false, performs dry-run only. |
| `bank` | `string` | no | *(none)* | Optional memory bank to operate on. Resolution precedence: explicit 'bank' argument, then the MNEMOSYNE_MCP_BANK environment variable, then 'default'. Omit to use MNEMOSYNE_MCP_BANK when set, otherwise the default bank (backward compatible). Named banks use separate local SQLite files and do not cross-read or cross-write. |

### 11. `mnemosyne_import`

Import Mnemosyne memories from a JSON file or another memory provider (Hindsight, Mem0). Idempotent by default.

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `input_path` | `string` | no | *(none)* | File path to read the export JSON from (for file imports) |
| `provider` | `string` | no | *(none)* | Provider to import from: 'hindsight', 'mem0'. Requires api_key. |
| `api_key` | `string` | no | *(none)* | API key for the source provider (can also be set via env var) |
| `user_id` | `string` | no | *(none)* | Filter imported memories by user ID (provider-specific) |
| `agent_id` | `string` | no | *(none)* | Filter imported memories by agent ID (provider-specific) |
| `base_url` | `string` | no | *(none)* | Base URL for self-hosted provider instances |
| `dry_run` | `boolean` | no | `false` | If true, validate and transform but don't write any memories |
| `channel_id` | `string` | no | *(none)* | Channel to assign imported memories to |
| `force` | `boolean` | no | `false` | If true, overwrite existing records instead of skipping |
| `bank` | `string` | no | *(none)* | Optional memory bank to operate on. Resolution precedence: explicit 'bank' argument, then the MNEMOSYNE_MCP_BANK environment variable, then 'default'. Omit to use MNEMOSYNE_MCP_BANK when set, otherwise the default bank (backward compatible). Named banks use separate local SQLite files and do not cross-read or cross-write. |

### 12. `mnemosyne_invalidate`

Mark a memory as expired or superseded. Provide memory_id from recall results. Optionally provide a replacement_id that must resolve to a working-memory or episodic-memory record that is accessible in the current session or global scope to chain old to new. An unknown or out-of-scope target or replacement returns status: memory_not_found.

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `memory_id` | `string` | yes | *(none)* | ID of memory to invalidate. |
| `replacement_id` | `string` | no | *(unset)* | Optional new memory that replaces this one. |
| `bank` | `string` | no | *(none)* | Optional memory bank to operate on. Resolution precedence: explicit 'bank' argument, then the MNEMOSYNE_MCP_BANK environment variable, then 'default'. Omit to use MNEMOSYNE_MCP_BANK when set, otherwise the default bank (backward compatible). Named banks use separate local SQLite files and do not cross-read or cross-write. |

### 13. `mnemosyne_recall`

Search Mnemosyne for relevant memories. Uses hybrid ranking: by default 50% vector similarity + 30% FTS5 text rank + 20% importance + optional temporal boost. Tune the per-query weights via vec_weight, fts_weight, importance_weight (omit or pass null to resolve config.yaml, then environment variables, then built-in defaults). Returns ranked results.

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `query` | `string` | yes | *(none)* | Natural language query. |
| `limit` | `integer` | no | `5` | Max results. Default 5. |
| `temporal_weight` | `number` | no | `0.0` | How much to boost recent memories (0.0 = ignore time, 0.2 = mild recency bias, 0.5 = strong recency bias). Default 0.0. |
| `query_time` | `string` | no | *(none)* | ISO timestamp to treat as 'now' for temporal scoring. Omit to use the current time. |
| `temporal_halflife` | `number` | no | `24` | Hours until temporal boost decays by half. Default 24. Lower = faster decay. |
| `vec_weight` | `number \| null` | no | *(none)* | Vector similarity weight in hybrid scoring. Omit (or pass null) to resolve config.yaml, then MNEMOSYNE_VEC_WEIGHT, then built-in default 0.5. |
| `fts_weight` | `number \| null` | no | *(none)* | Full-text search weight in hybrid scoring. Omit (or pass null) to resolve config.yaml, then MNEMOSYNE_FTS_WEIGHT, then built-in default 0.3. |
| `importance_weight` | `number \| null` | no | *(none)* | Importance score weight in hybrid scoring. Omit (or pass null) to resolve config.yaml, then MNEMOSYNE_IMPORTANCE_WEIGHT, then built-in default 0.2. |
| `explain` | `boolean` | no | `false` | If true, return a structured per-query recall explain trace. Default false. |
| `bank` | `string` | no | *(none)* | Optional memory bank to operate on. Resolution precedence: explicit 'bank' argument, then the MNEMOSYNE_MCP_BANK environment variable, then 'default'. Omit to use MNEMOSYNE_MCP_BANK when set, otherwise the default bank (backward compatible). Named banks use separate local SQLite files and do not cross-read or cross-write. |

### 14. `mnemosyne_recall_canonical`

Read CANONICAL self-facts for the current profile. With category+name: return the single authoritative value for that slot. With category only: list that category's slots. With query: substring-search the profile's canonical values. With nothing: list all canonical slots. Set include_history=true to also return superseded versions of a slot.

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `category` | `string` | no | *(unset)* |  |
| `name` | `string` | no | *(unset)* |  |
| `query` | `string` | no | *(unset)* | Substring search across the profile's canonical values |
| `include_history` | `boolean` | no | `false` | Include superseded versions (requires category+name) |
| `limit` | `integer` | no | `10` | Max results for query/list modes |
| `bank` | `string` | no | *(none)* | Optional memory bank to operate on. Resolution precedence: explicit 'bank' argument, then the MNEMOSYNE_MCP_BANK environment variable, then 'default'. Omit to use MNEMOSYNE_MCP_BANK when set, otherwise the default bank (backward compatible). Named banks use separate local SQLite files and do not cross-read or cross-write. |

### 15. `mnemosyne_remember`

Store a durable memory in Mnemosyne. Use for ANY fact, preference, identity, insight, or context that should persist across sessions. Higher importance (0.0-1.0) surfaces the memory more often. Use scope='global' for user-level facts; scope='session' for conversation-specific context. Use valid_until (ISO date YYYY-MM-DD) for time-bound facts. Use extract_entities=True to extract named entities for fuzzy recall (e.g. 'Abdias' and 'Abdias J.' will match). Use extract=True to also pull subject-predicate-object fact triples via LLM for fact-aware recall. Use veracity to tag confidence: 'stated' for direct user assertions, 'tool' for deterministic tool output, 'inferred' for derived guesses; 'unknown' (default) gets no recall boost.

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `content` | `string` | yes | *(none)* | The memory content to store. |
| `importance` | `number` | no | `0.5` | Importance 0.0-1.0. Default 0.5. |
| `source` | `string` | no | `user` | Source tag: preference, fact, insight, identity, task, etc. |
| `scope` | `string` | no | `session` | 'session' (default) or 'global'. |
| `valid_until` | `string` | no | *(unset)* | Optional expiry date YYYY-MM-DD. |
| `extract_entities` | `boolean` | no | `false` | Extract named entities for fuzzy recall. Default False. |
| `extract` | `boolean` | no | `false` | Extract subject-predicate-object fact triples via LLM for fact-aware recall. Default False. |
| `metadata` | `object` | no | `{}` | Optional dict of additional fields (source_doc, tags, page, etc.). Default empty. |
| `veracity` | `string` | no | `unknown` | Confidence label: 'stated' \| 'inferred' \| 'tool' \| 'imported' \| 'unknown'. Default 'unknown'. |
| `bank` | `string` | no | *(none)* | Optional memory bank to operate on. Resolution precedence: explicit 'bank' argument, then the MNEMOSYNE_MCP_BANK environment variable, then 'default'. Omit to use MNEMOSYNE_MCP_BANK when set, otherwise the default bank (backward compatible). Named banks use separate local SQLite files and do not cross-read or cross-write. |

### 16. `mnemosyne_remember_canonical`

Store a CANONICAL (single-source-of-truth) self-fact for the current profile. Each (category, name) slot holds exactly one current value: restating the same body is a no-op, and a new body supersedes the old one (kept as history). Use for stable identity cards — name, voice, stable preferences, relationships — that must not contradict themselves over time. Scoped privately to this profile. For relational facts use mnemosyne_triple_add; for episodic recall use mnemosyne_remember.

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `category` | `string` | yes | *(none)* | Slot group, e.g. 'identity', 'voice', 'preference' |
| `name` | `string` | yes | *(none)* | Slot key within the category, e.g. 'name', 'pronouns' |
| `body` | `string` | yes | *(none)* | The authoritative free-text value for this slot |
| `source` | `string` | no | *(unset)* | Optional provenance label |
| `confidence` | `number` | no | `1.0` | Optional 0..1 confidence |
| `bank` | `string` | no | *(none)* | Optional memory bank to operate on. Resolution precedence: explicit 'bank' argument, then the MNEMOSYNE_MCP_BANK environment variable, then 'default'. Omit to use MNEMOSYNE_MCP_BANK when set, otherwise the default bank (backward compatible). Named banks use separate local SQLite files and do not cross-read or cross-write. |

### 17. `mnemosyne_scratchpad_clear`

Clear all entries from the Mnemosyne scratchpad.

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `bank` | `string` | no | *(none)* | Optional memory bank to operate on. Resolution precedence: explicit 'bank' argument, then the MNEMOSYNE_MCP_BANK environment variable, then 'default'. Omit to use MNEMOSYNE_MCP_BANK when set, otherwise the default bank (backward compatible). Named banks use separate local SQLite files and do not cross-read or cross-write. |

### 18. `mnemosyne_scratchpad_read`

Read the Mnemosyne scratchpad entries.

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `bank` | `string` | no | *(none)* | Optional memory bank to operate on. Resolution precedence: explicit 'bank' argument, then the MNEMOSYNE_MCP_BANK environment variable, then 'default'. Omit to use MNEMOSYNE_MCP_BANK when set, otherwise the default bank (backward compatible). Named banks use separate local SQLite files and do not cross-read or cross-write. |

### 19. `mnemosyne_scratchpad_write`

Write a temporary note to the Mnemosyne scratchpad.

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `content` | `string` | yes | *(none)* | Content to write |
| `bank` | `string` | no | *(none)* | Optional memory bank to operate on. Resolution precedence: explicit 'bank' argument, then the MNEMOSYNE_MCP_BANK environment variable, then 'default'. Omit to use MNEMOSYNE_MCP_BANK when set, otherwise the default bank (backward compatible). Named banks use separate local SQLite files and do not cross-read or cross-write. |

### 20. `mnemosyne_shared_forget`

Delete one working shared-surface memory by exact ID.

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `memory_id` | `string` | yes | *(none)* |  |

### 21. `mnemosyne_shared_recall`

Search only the shared Mnemosyne surface DB.

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `query` | `string` | yes | *(none)* |  |
| `limit` | `integer` | no | `5` |  |

### 22. `mnemosyne_shared_remember`

Store compact cross-agent surface memory in a dedicated shared Mnemosyne DB. Use only for stable user/system/workflow metadata or general preferences. Normal mnemosyne_remember writes stay private.

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `content` | `string` | yes | *(none)* | Surface memory content to store. |
| `kind` | `string` | no | `meta` | meta \| preference \| correction \| identity |
| `importance` | `number` | no | `0.8` | Importance 0.0-1.0. Default 0.8. |
| `veracity` | `string` | no | `unknown` | stated \| inferred \| tool \| imported \| unknown |
| `metadata` | `object` | no | `{}` | Optional metadata object. |

### 23. `mnemosyne_shared_stats`

Return shared surface DB path and counts.

*No parameters.*

### 24. `mnemosyne_sleep`

Run the Mnemosyne consolidation cycle. Compresses old working memories into episodic summaries. Call after long sessions or when memory feels stale. Set all_sessions=true to consolidate eligible old working memories across inactive sessions.

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `all_sessions` | `boolean` | no | `false` | If true, consolidate eligible old working memories across all sessions instead of only the current session. |
| `dry_run` | `boolean` | no | `false` | If true, report what would be consolidated without writing changes. |
| `force` | `boolean` | no | `false` | If true, skip the age threshold and consolidate all non-consolidated working memories immediately. |
| `bank` | `string` | no | *(none)* | Optional memory bank to operate on. Resolution precedence: explicit 'bank' argument, then the MNEMOSYNE_MCP_BANK environment variable, then 'default'. Omit to use MNEMOSYNE_MCP_BANK when set, otherwise the default bank (backward compatible). Named banks use separate local SQLite files and do not cross-read or cross-write. |

### 25. `mnemosyne_stats`

Return Mnemosyne memory statistics: working count, episodic count, BEAM tiers.

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `bank` | `string` | no | *(none)* | Optional memory bank to operate on. Resolution precedence: explicit 'bank' argument, then the MNEMOSYNE_MCP_BANK environment variable, then 'default'. Omit to use MNEMOSYNE_MCP_BANK when set, otherwise the default bank (backward compatible). Named banks use separate local SQLite files and do not cross-read or cross-write. |

### 26. `mnemosyne_triple_add`

Add a temporal fact triple (subject, predicate, object) to the knowledge graph. Example: ('user', 'prefers', 'neovim'). Use for structured relationships. By default a new triple supersedes any prior fact with the same subject+predicate; set supersede=false for multi-valued facts that should coexist (e.g. ('user','speaks','English') and ('user','speaks','Spanish')).

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `subject` | `string` | yes | *(none)* |  |
| `predicate` | `string` | yes | *(none)* |  |
| `object` | `string` | yes | *(none)* |  |
| `valid_from` | `string` | no | *(unset)* | ISO date YYYY-MM-DD |
| `valid_until` | `string` | no | *(unset)* | Optional ISO expiry date YYYY-MM-DD. |
| `source` | `string` | no | *(unset)* | Provenance label. |
| `confidence` | `number` | no | `1.0` | 0.0-1.0 (default 1.0). |
| `supersede` | `boolean` | no | `true` | If false, do not close prior same subject+predicate triples (multi-valued). |
| `bank` | `string` | no | *(none)* | Optional memory bank to operate on. Resolution precedence: explicit 'bank' argument, then the MNEMOSYNE_MCP_BANK environment variable, then 'default'. Omit to use MNEMOSYNE_MCP_BANK when set, otherwise the default bank (backward compatible). Named banks use separate local SQLite files and do not cross-read or cross-write. |

### 27. `mnemosyne_triple_query`

Query the temporal knowledge graph for facts matching subject/predicate/object patterns. Subject match is case-insensitive. Pass as_of to query facts valid on a past date.

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `subject` | `string` | no | *(unset)* |  |
| `predicate` | `string` | no | *(unset)* |  |
| `object` | `string` | no | *(unset)* |  |
| `as_of` | `string` | no | *(unset)* | ISO date YYYY-MM-DD; query facts valid as of this date (default: today). |
| `bank` | `string` | no | *(none)* | Optional memory bank to operate on. Resolution precedence: explicit 'bank' argument, then the MNEMOSYNE_MCP_BANK environment variable, then 'default'. Omit to use MNEMOSYNE_MCP_BANK when set, otherwise the default bank (backward compatible). Named banks use separate local SQLite files and do not cross-read or cross-write. |

### 28. `mnemosyne_update`

Update the content or importance of an existing memory by ID.

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `memory_id` | `string` | yes | *(none)* | ID of the memory to update |
| `content` | `string` | no | *(none)* | New content for the memory (optional) |
| `importance` | `number` | no | *(none)* | New importance from 0.0 to 1.0 (optional) |
| `bank` | `string` | no | *(none)* | Optional memory bank to operate on. Resolution precedence: explicit 'bank' argument, then the MNEMOSYNE_MCP_BANK environment variable, then 'default'. Omit to use MNEMOSYNE_MCP_BANK when set, otherwise the default bank (backward compatible). Named banks use separate local SQLite files and do not cross-read or cross-write. |

### 29. `mnemosyne_validate`

Attest, update, or invalidate a memory the caller did not necessarily author. Supports collaborative ownership: any agent can validate any memory in either the private bank or the shared surface. The original author is preserved; validator + validated_at are updated to record the most recent attester. A 3-entry ring buffer keeps lightweight history. Actions: 'attest' (confirm correctness), 'update' (replace content), 'invalidate' (mark superseded), 'delete' (remove).

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `memory_id` | `string` | yes | *(none)* | ID of memory to validate. |
| `action` | `enum[attest, update, invalidate, delete]` | yes | *(none)* | What kind of validation to record. |
| `validator` | `string` | no | *(unset)* | Agent identifier performing the validation. Defaults to the caller's agent_identity if not set. |
| `new_content` | `string` | no | *(unset)* | New content (only used with action='update'). |
| `note` | `string` | no | *(unset)* | Optional reason or evidence for this validation. |
| `bank` | `enum[private, surface]` | no | `private` | Which bank holds the memory. Default 'private'. |

---

## Hermes plugin only, NOT available over MCP (8)

These schemas exist in `mnemosyne/tool_schemas.py` and are implemented by the Hermes provider adapters under `hermes_memory_provider/`. They have no entry in `_TOOL_HANDLERS`, so an MCP client that calls them gets an error.

### 1. `mnemosyne_persona_demote`

Move a persona fact back to memoria_preferences (acting as a tombstone). Use when a previously-promoted rule no longer applies or was a one-off. Returns the demoted persona id and the new preference record location.

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `persona_id` | `integer` | yes | *(none)* | ID of the persona row to demote. |
| `reason` | `string` | no | *(unset)* | Optional reason for the demotion. |

### 2. `mnemosyne_persona_list`

List L3 persona facts, optionally filtered by tier and/or topic. Returns personas ordered by tier (permanent first), then by reinforcement count descending (most-used first).

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `tier` | `enum[permanent, long_term, working]` | no | *(none)* | Optional tier filter. |
| `topic` | `string` | no | *(none)* | Optional topic filter (exact match). |

### 3. `mnemosyne_persona_promote`

Promote a working or episodic memory into the L3 persona store (the memoria_persona table). Tier values: 'permanent', 'long_term' (default), 'working'. Tier is a classification label: it orders mnemosyne_persona_list output (permanent first) and affects nothing else. No automatic eviction or decay is implemented, and a fact leaves the store only by explicit demotion. Persona facts are not read by the system prompt path, which uses the opt-in persona.md file. Returns the new persona id.

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `memory_id` | `string` | yes | *(none)* | ID of the source memory in working_memory or episodic_memory. |
| `tier` | `enum[permanent, long_term, working]` | no | `long_term` | Classification tier for the promoted persona fact; affects mnemosyne_persona_list ordering only. |
| `reason` | `string` | no | *(unset)* | Optional human-readable reason for the promotion (logged for audit). |

### 4. `mnemosyne_persona_reinforce`

Bump the reinforcement_count and last_reinforced_at on a persona fact. Use when the persona rule was just applied -- signals 'this rule is in active use'. Reinforcement count breaks ties in mnemosyne_persona_list ordering; it does not feed any decay logic, because none is implemented.

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `persona_id` | `integer` | yes | *(none)* | ID of the persona row to reinforce. |

### 5. `mnemosyne_sync_pull`

Pull remote memory changes from the configured Mnemosyne sync server. Applies incoming events locally with timestamp + importance conflict resolution.

*No parameters.*

### 6. `mnemosyne_sync_push`

Push local memory changes to a remote Mnemosyne sync server. Only events created since the last sync are sent. Requires a configured remote sync server (configured via config.yaml or MNEMOSYNE_SYNC_REMOTE env var).

*No parameters.*

### 7. `mnemosyne_sync_status`

Show Mnemosyne sync status: device ID, last cursor, event count, remote URL, and encryption state.

*No parameters.*

### 8. `mnemosyne_triple_end`

Expire a fact in the knowledge graph WITHOUT replacing it (e.g. a relationship that simply ended). Closes all open triples for subject+predicate, or only the one matching object when given. Use mnemosyne_triple_add instead when a new value replaces the old.

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `subject` | `string` | yes | *(none)* |  |
| `predicate` | `string` | yes | *(none)* |  |
| `object` | `string` | no | *(unset)* | Optional: end only this exact triple; omit to end all open subject+predicate triples. |
| `valid_until` | `string` | no | *(unset)* | ISO date YYYY-MM-DD the fact ended (default: today). |
